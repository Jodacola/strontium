export default class EnvironmentUtility {
    private static checked;
    private static alreadyChecked(key);
    private static addChecked(key);
    private static updateOnFirst(key, loaded);
    static jqueryLoaded(): boolean;
    static bootstrapLoaded(): boolean;
}
